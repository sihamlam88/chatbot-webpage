{
  "name": "my-chatbot-app",
  "version": "1.0.0",
  "description": "A simple chatbot app using OpenAI API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "dotenv": "^16.0.0",
    "node-fetch": "^2.6.1"  // Used to make API requests, replace with native fetch if using Node 18+
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
